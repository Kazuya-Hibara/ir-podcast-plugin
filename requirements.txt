# NotebookLM Python CLI (Playwright cookie auth ベース)
# 上流: https://github.com/teng-lin/notebooklm-py (MIT)
notebooklm-py>=0.3.4,<0.5.0

# HTTP client for SEC EDGAR / EDINET API
httpx>=0.27,<1.0

# YAML loading for companies.yaml watchlist
PyYAML>=6.0,<7.0

# Date parsing (filing dates, EDINET 日付フォーマット)
python-dateutil>=2.9,<3.0

# 注意:
# - playwright は notebooklm-py[browser] extra で install される
# - 別途 `playwright install chromium` で browser binary を取得
# - PDF parsing は pdftotext (poppler-utils) を使う、Python lib は不使用
